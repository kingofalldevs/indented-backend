services:
  - type: web
    name: indented-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: GROQ_API_KEY
        sync: false # Set manually in Render Dashboard for security
      - key: PORT
        value: 5051
